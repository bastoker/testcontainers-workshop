management.security.enabled: false
server:
  port: 8080
spring:
  datasource:
    url: 'jdbc:postgresql://localhost:54425/integration-tests-db?loggerLevel=OFF'
    username: 'admin'
    password: '@dm1n'
keycloak:
  auth-server-url: 'http://localhost:54433/'
  realm: vakantieplanner
  resource: spring-app
  use-resource-role-mappings: false
  bearer-only: true


