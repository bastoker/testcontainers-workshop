server:
  port: 8080
  error:
    include-message: always
spring:
  datasource:
    url: 'jdbc:postgresql://localhost:52975/standalone-db?loggerLevel=OFF'
    username: 'admin'
    password: 'admin'
keycloak:
  auth-server-url: 'http://localhost:53172/'
  realm: vakantieplanner
  resource: spring-app
  use-resource-role-mappings: false
  bearer-only: true


